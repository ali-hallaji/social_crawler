# import datetime
# from config.settings import BASE_DIR
# from services.libs.data_cache import DataCache
# bcode = DataCache(datetime.timedelta(0, 30, 0))
# bcode.setName('BCode')
# token = DataCache(datetime.timedelta(15, 0, 0, 0, 0))
# token.setName('Token')
